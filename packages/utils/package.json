{
  "name": "@repo/utils",
  "version": "1.0.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "lint": "echo lint utils ok"
  },
  "devDependencies": {
    "typescript": "latest"
  }
}
